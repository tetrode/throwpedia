{
    "name": "tetrode/throwpedia",
    "bin": ["bin/throwpedia"],
    "description": "Throwpedia: the business exception library",
    "type": "project",
    "require": {
        "php": "~8.4.0",
        "nikic/php-parser": "^5.0",
        "nette/neon": "^3.4"
    },
    "suggest": {
        "ext-dom": "Required to use the XML output format",
        "ext-simplexml": "Required to use the XML output format"
    },
    "require-dev": {
        "phpunit/phpunit": "^12.5",
        "friendsofphp/php-cs-fixer": "^v3",
        "phpstan/phpstan": "^2",
        "phpstan/phpstan-strict-rules": "^2.0",
        "phpstan/phpstan-phpunit": "^2.0",
        "php-parallel-lint/php-parallel-lint": "^1.4",
        "maglnet/composer-require-checker": "^4.24",
        "rector/rector": "^2.3"
    },
    "autoload": {
        "psr-4": {
            "Tetrode\\Throwpedia\\": "src"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tetrode\\Throwpedia\\Tests\\": "tests"
        }
    },
    "config": {
        "platform": {
            "php": "8.4.20"
        },
        "allow-plugins": {
            "infection/extension-installer": true
        }
    },
    "scripts": {
        "test": "phpunit",
        "cs": "php-cs-fixer fix",
        "stan": "phpstan analyze --memory-limit=1G",
        "lint": "parallel-lint src tests --colors",
        "req": "composer-require-checker",
        "all": "composer lint && composer test && composer cs && composer stan && composer req && composer audit"
    }
}