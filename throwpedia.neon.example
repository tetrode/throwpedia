# Configuration for Throwpedia

# Sources
source:
    - src

# Exception Attributes
# You can provide a simple list of attribute names (they will use the global 'fields' or defaults):
# attributes:
#     - ExceptionReason
#     - AuditReason
#
# OR provide a map of attribute names to their specific fields:
attributes:
    ExceptionReason:
        code: Code
        technicalReason: Technical Reason
        businessReason: Business Reason
    AuditReason:
        code: Audit ID
        action: Audit Action

# Attribute Fields (optional, defaults to code, technicalReason, businessReason)
fields:
    code: Code
    technicalReason: Technical Reason
    businessReason: Business Reason

# Output files. Remove the ones that you do not need
outputs:
    - ./output/exceptions.json
    - ./output/exceptions.yaml
    - ./output/exceptions.md

# Is throw new Exception() allowed or is only throw MyException::Method() allowed
allowDirectNew: false

# Suppress duplicate code warnings (optional)
suppressDuplicateCodeWarning: false
